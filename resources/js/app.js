require('jquery'); //npm install jquery
require('bootstrap'); //npm install bootstrap && pm install popper.js --save
import Vue from 'vue'; //npm install vue


new Vue({
  el:'#app',
  name:'MainApp'
});

console.log("app.js loded");